@{ Layout = "/Views/Shared/_Layout.cshtml"; ViewBag.PageClass = "container";  }
        <p class="lead">@LoginLocalizer["Configuração da sua conta"].</p>
        <hr class="my-4">
        <div class="row">
            <div class="col-md-3">
                @await Html.PartialAsync("_ManageNav")
            </div>
            <div class="col-md-9">
                @RenderBody()
            </div>
        </div>
@section Scripts {
    @RenderSection("Scripts", required: false)
}
